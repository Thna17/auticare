# API Documentation

OpenAPI is served locally at `http://localhost:3000/api/docs`. API routes are versioned under `/api/v1` and use `{ data, meta }` success envelopes plus `{ error }` failure envelopes.
