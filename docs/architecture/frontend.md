# Frontend Architecture

Angular uses standalone components, route-level lazy loading, reactive forms, signals for synchronous UI state, RxJS for HTTP streams, functional guards, functional interceptors, and SCSS design tokens. Reusable components live in the design system only when their behavior and accessibility rules are stable.
